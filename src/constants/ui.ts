// BREAKPOINTS moved to centralized.css as CSS variables
// Use var(--breakpoint-mobile), var(--breakpoint-tablet), etc. instead

// Z_INDEX moved to centralized.css as CSS variables
// Use var(--z-dropdown), var(--z-modal), etc. instead

// ANIMATION_DURATION moved to centralized.css as CSS variables
// Use var(--duration-fast), var(--duration-normal), etc. instead

// COLORS moved to centralized.css as CSS variables
// Use var(--brand-primary), var(--brand-success), etc. instead

// SPACING moved to centralized.css as CSS variables
// Use var(--space-1), var(--space-2), etc. or Tailwind classes instead

// BORDER_RADIUS moved to centralized.css as CSS variables
// Use var(--radius-none), var(--radius-sm), etc. instead

// FONT_SIZES and FONT_WEIGHTS moved to centralized.css as CSS variables
// Use var(--font-size-xs), var(--font-weight-normal), etc. instead

// SHADOWS moved to centralized.css as CSS variables
// Use var(--shadow-none), var(--shadow-sm), etc. instead
