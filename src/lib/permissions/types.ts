/**
 * Permission types (client-safe)
 * Types only - no server dependencies
 */

// Permission ID is a string key from PERMISSIONS object
export type PermissionId = string;

// Role ID is a string (roles are managed in Database)
export type RoleId = string;

